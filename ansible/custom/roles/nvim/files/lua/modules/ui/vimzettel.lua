-- local ok, vimzettel = pcall(require, "zettel")
-- if not ok then
-- 	local errmsg = "[UI] `vim-zettel` plugin not installed! Please install via your plugin manager."
-- 	vim.api.nvim_err_writeln(errmsg)
-- 	return
-- end

vim.g.nv_search_paths = "~/zettelkasten"

-- require("vim-zettel").setup({})
