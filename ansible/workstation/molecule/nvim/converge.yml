---
- name: Converge
  hosts: all

  # tasks:
  #   - name: Include role
  #     include_role:
  #       name: role-name

  # collections:
  #   - source: './ansible'
  #     type: subdir

  roles:
    - role: 'oswee.workstation.nvim'
