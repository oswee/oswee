---
# vars file for vagrant

packages:
  redhat:
    - gcc
    - make
    # - libvirt
    # - libvirt-devel
    # - libxml2-devel
    - libxslt-devel
    - libguestfs-tools-c
    - ruby-devel
    # - "{{ vagrant_rpm }}"
