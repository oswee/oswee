---
# tasks file for dhcp

- name: Install packages
  become: true
  ansible.builtin.dnf:
    name: '{{ item }}'
    state: present
  notify:
    - start dhcpd
  loop:
    # - dhcp
    - dhcp-server
...
