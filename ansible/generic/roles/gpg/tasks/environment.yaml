---
# tasks file for gpg

- name: environment | Add environment
  become: true
  ansible.builtin.copy:
    dest: /etc/profile.d/gpg2.sh
    mode: '0644'
    content: |
      export GPG_TTY=$(tty)  # To enable Git commit signing
...
