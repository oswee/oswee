---
# tasks file for zsh

- name: Install
  import_tasks: install.yaml
  tags:
    - zsh:setup
    - zsh:setup:install

- name: Config
  import_tasks: configure.yaml
  tags:
    - zsh:setup
    - zsh:setup:config
# - name: Remove
#   import_tasks: remove.yaml
#   tags:
#     - zsh:remove
