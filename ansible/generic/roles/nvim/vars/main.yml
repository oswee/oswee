---
# vars file for nvim

nvim_build_packages:
  redhat:
    - git
    - ninja-build
    - libtool
    - autoconf
    - automake
    - cmake
    - gcc
    - gcc-c++
    - make
    - pkgconfig
    - unzip
    - patch
    - gettext

packages:
  redhat:
    # - python3-neovim
    - python3-pip
    - fzf
    - ripgrep
    - the_silver_searcher
    - bat  # for the pretty fzf previews
    - yamllint
    - wget
    - fd-find
