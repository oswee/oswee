---
# tasks file for minio

- name: Install
  import_tasks: install.yaml
  tags:
    - minio:setup
    - minio:setup:install
# - name: Config
#   import_tasks: configure.yaml
#   tags:
#     - minio:setup
#     - minio:setup:config

# - name: Cleanup
#   import_tasks: cleanup.yaml
#   tags:
#     - minio:cleanup
