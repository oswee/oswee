---
apiVersion: v1
kind: Service
metadata:
  name: web-server-service
  namespace: prime
spec:
  # type: LoadBalancer
  selector:
    app: web-server
  ports:
    # - name: http
    #   protocol: TCP
    #   port: 80 # Host
    #   targetPort: http
    - name: https
      protocol: TCP
      port: 443 # Host
      targetPort: http
