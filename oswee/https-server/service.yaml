---
apiVersion: v1
kind: Service
metadata:
  name: https-server-service
  namespace: prime
  labels:
    app: https-server
spec:
  selector:
    app: https-server
  ports:
    - name: http
      protocol: TCP
      port: 8000  # Host
      targetPort: 8080  # Container
