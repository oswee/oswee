
load("@npm//@bazel/typescript:index.bzl", "ts_config", "ts_library")

package(default_visibility = ["//visibility:public"])

ts_library(
    name = "websocket",
    srcs = glob(
        include = ["src/**/*.ts"],
        exclude = [
            "src/**/*.spec.ts",
            "src/test-setup.ts",
        ],
    ),
    # module_name = "@oswee/elements/avatar",
    module_root = "src",
    # tsconfig = "tsconfig_lib",
    deps = [
        # "@oswee//apps/front/src/store:store",
        "@oswee//libs/router:router",
        "@npm//google-protobuf",
        "@npm//redux",
        "@npm//redux-saga",
        "@npm//universal-router",
        "@npm//reselect",
        "@npm//history",
        "@npm//tslib",
    ],
    data = [
        "@oswee//apps/front:src/types.d.ts",
        ]
)

ts_config(
    name = "tsconfig_lib",
    src = "tsconfig.lib.json",
    deps = [
        "tsconfig.json",
    ],
)
