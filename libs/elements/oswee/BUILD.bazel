load("@npm//@bazel/typescript:index.bzl", "ts_config")

package(default_visibility = ["//oswee:__subpackages__"])

ts_config(
    name = "tsconfig",
    src = "tsconfig.json",
    deps = [
        "//:tsconfig.base.json",
    ],
)
