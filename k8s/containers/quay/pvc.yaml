---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-pvc-quay-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeName: registry-pv
...
