---
# https://ansible-lint.readthedocs.io/configuring/#ansible-lint-configuration
exclude_paths:
  - ~/.ansible  # Exclude external playbooks from linting
use_default_rules: true
offline: true
enable_list:
  - name[prefix]
# skip_list:
  # - internal-error
  # - name[casing]
...
