---
- name: Desktop Environment
  hosts: workstations
  become: false
  gather_facts: true


  tasks:
    - name: Libdisplay-info
      ansible.builtin.import_role:
        name: oswee.generic.libdisplay_info
      tags:
        - libdisplay_info

    - name: Libliftoff
      ansible.builtin.import_role:
        name: oswee.generic.libliftoff
      tags:
        - libliftoff

    - name: Libxcb-errors
      ansible.builtin.import_role:
        name: oswee.generic.libxcb_errors
      tags:
        - libxcb_errors

    - name: Pixman
      ansible.builtin.import_role:
        name: oswee.generic.pixman
      tags:
        - pixman

    - name: Wlroots
      ansible.builtin.import_role:
        name: oswee.generic.wlroots
      tags:
        - wlroots

    - name: Hyprland
      ansible.builtin.import_role:
        name: oswee.generic.hyprland
      tags:
        - hyprland

    - name: Kitty
      ansible.builtin.import_role:
        name: oswee.generic.kitty
      tags:
        - kitty

    - name: Waybar
      ansible.builtin.import_role:
        name: oswee.generic.waybar
      tags:
        - waybar
...
