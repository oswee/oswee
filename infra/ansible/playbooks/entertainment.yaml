---
- name: Entertainment
  hosts: workstations
  connection: local
  become: false
  gather_facts: true

  tasks:
    - name: OBS
      ansible.builtin.import_role:
        name: oswee.generic.obs
      tags:
        - obs

    - name: Glfw
      ansible.builtin.import_role:
        name: oswee.generic.glfw
      tags:
        - glfw

    - name: Minecraft
      ansible.builtin.import_role:
        name: oswee.custom.minecraft
      tags:
        - minecraft
...
