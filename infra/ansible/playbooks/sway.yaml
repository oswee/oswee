---
- name: Sway Window Manager
  hosts: workstations
  become: false
  gather_facts: true


  tasks:
    - name: Sway
      ansible.builtin.import_role:
        name: oswee.custom.sway
      tags:
        - sway

    - name: Eww
      ansible.builtin.import_role:
        name: oswee.custom.eww
      tags:
        - eww
...
