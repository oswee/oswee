---
- name: Desktop Environment
  hosts: workstations
  become: false
  gather_facts: true


  tasks:
    - name: AMD
      ansible.builtin.import_role:
        name: oswee.generic.amd
      tags:
        - amd

    - name: Fonts
      ansible.builtin.import_role:
        name: oswee.custom.fonts
      tags:
        - fonts

    - name: Kitty
      ansible.builtin.import_role:
        name: oswee.generic.kitty
      tags:
        - kitty

    # - name: Sway-idle
    # - name: Background
    # - name: Waybar
...
