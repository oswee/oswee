---
- name: Configure Build Server
  hosts: workstations
  become: false
  gather_facts: true

  tasks:
    # - name: Firewall
    #   ansible.builtin.import_role:
    #     name: oswee.generic.firewalld
    #
    # - name: Buildkite
    #   ansible.builtin.import_role:
    #     name: oswee.generic.buildkite

    - name: Bazel
      block:
        - name: Install Bazel
          ansible.builtin.import_role:
            name: oswee.generic.bazel
        # - name: Install Bazel Remote Cache
        #   ansible.builtin.import_role:
        #     name: oswee.generic.bazelremote
...
