---
- name: Configure Build Server
  hosts: build
  # connection: local
  become: false
  # become_user: ansible
  gather_facts: true

  tasks:
    - name: Firewall
      ansible.builtin.import_role:
        name: oswee.generic.firewalld

    - name: Bazel
      block:
        - name: Install Bazel Remote Cache
          ansible.builtin.import_role:
            name: oswee.generic.bazelremote
...
