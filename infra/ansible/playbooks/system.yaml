---
- name: Setup Base
  hosts: all
  become: false
  gather_facts: true

  tasks:
    - name: Firewalld
      ansible.builtin.import_role:
        name: oswee.generic.firewalld
      tags:
        - firewalld

# Base tools
  # Firewall
  # SSH
  # Updates
  # Backups

# Hardening
  # Rsync
  # SSH
...
