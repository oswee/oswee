---
# tasks/install.yaml file for oswee.generic.direnv role

- name: install | Install packages
  become: true
  ansible.builtin.dnf:
    name: '{{ item }}'
    state: present
  loop:
    - '{{ packages.redhat }}'
...
