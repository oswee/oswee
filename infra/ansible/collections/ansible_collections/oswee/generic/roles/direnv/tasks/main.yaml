---
# tasks/main.yaml file for oswee.generic.direnv role

- name: Setup
  tags:
    - direnv.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - direnv.install

    - name: Environment
      ansible.builtin.import_tasks: environment.yaml
      tags:
        - exa.environment

# - name: Teardown
#   ansible.builtin.import_tasks: teardown.yaml
#   tags:
#     - never
#     - direnv.teardown
...
