---
# tasks/main.yaml file for oswee.generic.slack role

- name: Setup
  tags:
    - slack.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - slack.install

- name: Teardown
  ansible.builtin.import_tasks: teardown.yaml
  tags:
    - never
    - slack.teardown
...
