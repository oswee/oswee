---
# tasks file for Lazygit

- name: install | Enable Lazygit Copr repository
  become: true
  community.general.copr:
    host: copr.fedorainfracloud.org
    state: enabled
    name: atim/lazygit
    # chroot: fedora-37-x86_64

- name: install | Install Lazygit
  become: true
  ansible.builtin.dnf:
    name: lazygit
    state: present
...
