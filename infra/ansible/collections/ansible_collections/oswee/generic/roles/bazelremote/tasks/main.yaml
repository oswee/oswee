---
# tasks file for Bazelremote

- name: Setup
  tags:
    - bazelremote
    - bazelremote.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - install
        - bazelremote.install
    # - name: Config
    #   ansible.builtin.import_tasks: config.yaml
    #   tags:
    #     - config
    #     - bazelremote.config

# - name: Teardown
#   ansible.builtin.import_tasks: teardown.yaml
#   tags:
#     - never
#     - teardown
#     - bazelremote.teardown
...
