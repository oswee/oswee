---
# tasks/main.yaml file for oswee.generic.swaylock role

- name: Setup
  tags:
    - swaylock.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - swaylock.install

# - name: Teardown
#   ansible.builtin.import_tasks: teardown.yaml
#   tags:
#     - never
#     - teardown
#     - swaylock.teardown
...
