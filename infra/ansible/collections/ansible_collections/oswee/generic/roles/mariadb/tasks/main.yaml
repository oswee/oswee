---
# tasks file for Mariadb

- name: Setup
  tags:
    - mariadb.setup
  block:
    - name: Install
      import_tasks: install.yaml
      tags:
        - mariadb.setup
        - mariadb.setup.install

    - name: Config
      import_tasks: config.yaml
      tags:
        - mariadb.setup
        - mariadb.setup.config

    - name: Secure installation
      import_tasks: secure-installation.yaml
      tags:
        - mariadb.setup
        - mariadb.setup.secinst

    - name: Database bootstrap
      import_tasks: databases.yaml
      tags:
        - mariadb.setup
        - mariadb.setup.databases

    - name: User setup
      import_tasks: users.yaml
      tags:
        - mariadb.setup
        - mariadb.setup.users

- name: Teardown MariaDB
  import_tasks: teardown.yaml
  tags:
    - never
    - mariadb.teardown
...
