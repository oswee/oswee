---
# tasks/main.yaml file for oswee.generic.sndio role

- name: Setup
  tags:
    - sndio.setup
  block:
    - name: Prepare
      ansible.builtin.import_tasks: prepare.yaml
      tags:
        - sndio.prepare

    - name: Build
      ansible.builtin.import_tasks: build.yaml
      tags:
        - sndio.build

    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - sndio.install

- name: Teardown
  ansible.builtin.import_tasks: teardown.yaml
  tags:
    - never
    - sndio.teardown
...
