---
# tasks/main.yaml file for oswee.generic.ovmf role

- name: Setup
  tags:
    - ovmf.setup
  block:
    - name: Prepare
      ansible.builtin.import_tasks: prepare.yaml
      tags:
        - ovmf.prepare

    - name: Build
      ansible.builtin.import_tasks: build.yaml
      tags:
        - ovmf.build

    - name: Config
      ansible.builtin.import_tasks: config.yaml
      tags:
        - ovmf.config

- name: Teardown
  ansible.builtin.import_tasks: teardown.yaml
  tags:
    - never
    - ovmf.teardown
...
