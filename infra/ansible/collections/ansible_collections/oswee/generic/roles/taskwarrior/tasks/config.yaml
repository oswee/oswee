---
# tasks file for Taskwarrior

- name: config | Write config
  ansible.builtin.template:
    src: '{{ item.src }}'
    dest: '{{ item.dest }}'
    mode: '{{ item.mode }}'
    backup: true
  loop:
    - { src: 'taskrc.j2', dest: '$HOME/.taskrc', mode: '0644' }
...
