---
# tasks file for Istio

- name: Setup
  block:
    - name: Install
      import_tasks: install.yaml
      tags:
        - install
        - istio.install
  tags:
    - istio
    - istio.setup

- name: Teardown
  import_tasks: teardown.yaml
  tags:
    - never
    - teardown
    - istio.teardown
...
