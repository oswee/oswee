---
# tasks file for Istio

- name: teardown | Delete Istio directory
  become: true
  ansible.builtin.file:
    state: absent
    path: /opt/istio/

- name: teardown | Remove Istio from system $PATH
  become: true
  ansible.builtin.file:
    state: absent
    path: /etc/profile.d/istio.sh
...
