---
# tasks file for istio

- name: Delete Istio directory
  become: true
  ansible.builtin.file:
    state: absent
    path: /opt/istio/
  tags:
    - istio.teardown

- name: Remove Istio from system $PATH
  become: true
  ansible.builtin.file:
    state: absent
    path: /etc/profile.d/istio.sh
  tags:
    - istio.teardown
...
