---
# tasks/main.yaml file for oswee.generic.vit role

- name: Setup
  tags:
    - vit.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - vit.install
    - name: Config
      ansible.builtin.import_tasks: config.yaml
      tags:
        - vit.config

# - name: Teardown
#   ansible.builtin.import_tasks: teardown.yaml
#   tags:
#     - never
#     - vit.teardown
...
