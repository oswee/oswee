---
# tasks file for Emacs

# TODO:
# - Emacs daemon systemd setup
# - Emacs GUI client setup
# - Emacs TTY client setup
# - Gruvbox theme

- name: Setup
  tags:
    - emacs
    - emacs.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - install
        - emacs.install
    - name: Config
      ansible.builtin.import_tasks: config.yaml
      tags:
        - config
        - emacs.config

- name: Teardown
  ansible.builtin.import_tasks: teardown.yaml
  tags:
    - never
    - teardown
    - emacs.teardown
...
