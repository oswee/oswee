---
# tasks file for Lua

- name: config | Add bash wrapper script
  ansible.builtin.copy:
    dest: /home/{{ lua_user }}/.local/bin/lua-language-server
    mode: 0755
    content: |
      #!/bin/bash
      exec "{{ lua_destination_dir }}/bin/lua-language-server" "$@"
...
