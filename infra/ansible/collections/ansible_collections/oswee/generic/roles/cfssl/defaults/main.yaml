---
# defaults file for Cfssl

cfssl_repo_url: https://github.com/cloudflare/cfssl.git
cfssl_source_dir: /home/{{ ansible_user_id }}/code/github.com/cloudflare/cfssl
cfssl_config_dir: $HOME/.config/cfssl
...
