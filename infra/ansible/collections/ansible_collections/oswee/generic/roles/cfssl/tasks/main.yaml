---
# tasks file for Cfssl

- name: Build
  import_tasks: build.yaml
  tags:
    - cfssl.build
    - cfssl.setup

- name: Install
  import_tasks: install.yaml
  tags:
    - cfssl.setup

- name: Teardown
  import_tasks: teardown.yaml
  tags:
    - cfssl.teardown
...
