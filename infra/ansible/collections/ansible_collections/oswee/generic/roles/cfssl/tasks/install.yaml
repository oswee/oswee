---
# tasks/install.yaml file for oswee.generic.cfssl role

- name: install | Copy CFSSL artifact binaries
  become: true
  ansible.builtin.copy:
    src: '{{ cfssl_source_dir }}/bin/'
    dest: /usr/local/bin
    remote_src: true
...
