---
# tasks/main.yaml file for oswee.generic.wlroots role

- name: Setup
  tags:
    - wlroots.setup
  block:
    - name: Prepare
      ansible.builtin.import_tasks: prepare.yaml
      tags:
        - wlroots.prepare

    - name: Build
      ansible.builtin.import_tasks: build.yaml
      tags:
        - wlroots.build

# - name: Teardown
#   ansible.builtin.import_tasks: teardown.yaml
#   tags:
#     - never
#     - wlroots.teardown
...
