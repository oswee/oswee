---
# tasks file for Haproxy

- name: Setup
  tags:
    - haproxy.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - haproxy.install

# - name: Teardown
#   import_tasks: teardown.yaml
#   tags:
#     - never
#     - haproxy.teardown
...
