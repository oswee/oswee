---
# tasks/main.yaml file for oswee.generic.haproxy role

- name: Setup
  tags:
    - haproxy.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - haproxy.install

    - name: Data Plane API
      ansible.builtin.import_tasks: dpapi.yaml
      tags:
        - haproxy.dpapi

# - name: Teardown
#   import_tasks: teardown.yaml
#   tags:
#     - never
#     - haproxy.teardown
...
