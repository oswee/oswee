---
# tasks/main.yaml file for oswee.generic.alacritty role

- name: Setup
  tags:
    - alacritty.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - alacritty.install

    - name: Config
      ansible.builtin.import_tasks: config.yaml
      tags:
        - alacritty.config

- name: Teardown
  ansible.builtin.import_tasks: teardown.yaml
  tags:
    - never
    - alacritty.teardown
...
