---
# tasks/environment.yaml file for oswee.generic.rust role

- name: environment | Add environment
  become: true
  ansible.builtin.copy:
    dest: /etc/profile.d/rust.sh
    mode: 0644
    content: |
      CARGOPATH=$HOME/.cargo/bin
      PATH=$CARGOPATH:$PATH
...
