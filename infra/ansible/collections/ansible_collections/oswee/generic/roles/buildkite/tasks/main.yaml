---
# tasks/main.yaml file for oswee.generic.buildkite role

- name: Setup
  tags:
    - buildkite.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - buildkite.install

    # - name: Config
    #   ansible.builtin.import_tasks: config.yaml
    #   tags:
    #     - buildkite.config

# - name: Teardown
#   import_tasks: teardown.yaml
#   tags:
#     - never
#     - buildkite.teardown
...
