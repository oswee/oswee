---
# tasks file for Vifm

- name: Setup
  block:
    - name: Install
      import_tasks: install.yaml
      tags:
        - install
        - vifm.install

    - name: Config
      import_tasks: config.yaml
      tags:
        - config
        - vifm.config
  tags:
    - vifm

- name: Teardown
  import_tasks: teardown.yaml
  tags:
    - never
    - teardown
    - vifm.teardown
...
