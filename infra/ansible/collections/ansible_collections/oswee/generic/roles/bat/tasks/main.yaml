---
# tasks file for Bat

- name: Setup
  tags:
    - bat
  block:
    - name: Install
      import_tasks: install.yaml
      tags:
        - install
        - bat.install

# - name: Teardown
#   import_tasks: teardown.yaml
#   tags:
#     - never
#     - teardown
#     - bat.teardown
...
