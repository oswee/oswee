---
# tasks file for Bat

- name: Setup
  block:
    - name: Install
      import_tasks: install.yaml
      tags:
        - install
        - bat.install
  tags:
    - bat

- name: Teardown
  import_tasks: teardown.yaml
  tags:
    - never
    - teardown
    - bat.teardown
...
