---
# tasks/prepare.yaml file for oswee.generic.hyprland role

- name: prepare | Install prerequisites
  become: true
  ansible.builtin.dnf:
    name: '{{ item }}'
    state: present
  loop: '{{ packages.redhat }}'

- name: prepare | Install hyprland dependencies
  become: true
  ansible.builtin.dnf:
    name: '{{ item }}'
    state: present
  loop: '{{ hyprland_deps.redhat }}'
...
