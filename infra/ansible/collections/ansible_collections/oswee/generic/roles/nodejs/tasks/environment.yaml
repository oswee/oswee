---
# tasks file for Nodejs

- name: environment | Setup NodeJS environment
  become: true
  ansible.builtin.copy:
    dest: /etc/profile.d/nodejs.sh
    mode: 0644
    content: |
      PATH={{ nodejs_dir }}/bin:$PATH
...
