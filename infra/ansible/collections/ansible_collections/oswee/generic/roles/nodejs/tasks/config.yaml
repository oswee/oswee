---
# tasks file for Nodejs

- name: config | Set NodeJS package prefix
  ansible.builtin.copy:
    dest: /home/{{ nodejs_user }}/.npmrc
    owner: '{{ nodejs_user }}'
    group: '{{ nodejs_group }}'
    mode: 0644
    content: |
      prefix=/home/{{ nodejs_user }}/.local
...
