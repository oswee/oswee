---
# tasks file for nodejs

- name: Setup
  block:
    - name: Install
      import_tasks: install.yaml
      tags:
        - install
        - nodejs.install
  tags:
    - nodejs

# - name: Teardown
#   import_tasks: teardown.yaml
#   tags:
#     - teardown
#     - nodejs.teardown
#     - never
...
