---
- name: Setup
  block:
    - name: Install
      import_tasks: install.yaml
      tags:
        - install
        - ansible.install

    - name: Config
      import_tasks: config.yaml
      tags:
        - config
        - ansible.config

  tags:
    - ansible
    - ansible.setup

# - name: Teardown
#   import_tasks: teardown.yaml
#   tags:
#     - teardown
#     - ansible.teardown
#     - never
...
