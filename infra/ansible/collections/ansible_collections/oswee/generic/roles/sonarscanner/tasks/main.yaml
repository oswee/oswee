---
# tasks file for Sonarscanner

- name: Setup
  block:
    - name: Install
      import_tasks: install.yaml
      tags:
        - install
        - sonarscanner.install
    - name: Config
      import_tasks: config.yaml
      tags:
        - config
        - sonarscanner.config
  tags:
    - sonarscanner
    - sonarscanner.setup

# - name: Teardown
#   import_tasks: teardown.yaml
#   tags:
#     - never
#     - teardown
#     - sonarscanner.teardown
...
