---
# tasks file for Nvim

- name: packges | Install complementary packages
  become: true
  ansible.builtin.dnf:
    name: '{{ item }}'
    state: present
  loop: '{{ packages.redhat }}'

- name: packges | Install NPM Neovim package globally
  community.general.yarn:
    name: neovim@latest
    global: true

- name: packges | Install pynvim python package
  ansible.builtin.pip:
    name: pynvim
    state: forcereinstall
...
