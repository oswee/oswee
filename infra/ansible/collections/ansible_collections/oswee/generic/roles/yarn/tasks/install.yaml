---
# tasks file for Yarn

- name: install | Install Yarn NPM package globally
  community.general.npm:
    # remote_user: vagrant
    name: yarn@latest
    global: true
  # environment:
  #   PATH: /opt/nodejs/bin:{{ ansible_facts.env.PATH }}
...
