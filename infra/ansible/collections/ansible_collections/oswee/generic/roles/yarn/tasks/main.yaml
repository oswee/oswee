---
# tasks file for Yarn

- name: Setup
  block:
    - name: Install
      import_tasks: install.yaml
      tags:
        - install
        - yarn.install

    - name: Config
      import_tasks: config.yaml
      tags:
        - config
        - yarn.config
  tags:
    - yarn
...
