---
# tasks file for Yarn

- name: Setup
  tags:
    - yarn.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - yarn.install

    - name: Config
      ansible.builtin.import_tasks: config.yaml
      tags:
        - yarn.config
...
