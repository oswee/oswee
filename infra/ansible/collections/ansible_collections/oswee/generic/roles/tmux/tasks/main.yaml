---
# tasks/main.yaml file for oswee.generic.tmux role

- name: Setup
  tags:
    - tmux.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - tmux.install
    - name: Config
      ansible.builtin.import_tasks: config.yaml
      tags:
        - tmux.config

# - name: Teardown
#   ansible.builtin.import_tasks: teardown.yaml
#   tags:
#     - never
#     - tmux.teardown
...
