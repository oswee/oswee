---
# tasks/teardown.yaml file for oswee.generic.tmux role

- name: teardown | Remove packages
  become: true
  ansible.builtin.dnf:
    name: '{{ item }}'
    state: absent
  loop: '{{ packages.redhat }}'
...
