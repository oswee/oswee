---
# tasks/main.yaml file for oswee.generic.sway role

- name: Setup
  tags:
    - sway.generic
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - sway.install

# - name: Teardown
#   ansible.builtin.import_tasks: teardown.yaml
#   tags:
#     - never
#     - sway.teardown
...
