---
# tasks/main.yaml file for oswee.generic.obs role

- name: Setup
  tags:
    - obs.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - obs.install

# - name: Teardown
#   ansible.builtin.import_tasks: teardown.yaml
#   tags:
#     - never
#     - tmux.teardown

...
