---
# tasks file for minikube

# `minikube -p dev stop`
# `minikube delete`

- name: Teardown Minikube
  become: true
  ansible.builtin.dnf:
    name: 'minikube'
    state: absent
  tags:
    - minikube.teardown
...
