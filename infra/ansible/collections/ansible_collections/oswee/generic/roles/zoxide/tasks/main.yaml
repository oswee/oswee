---
# tasks/main.yaml file for oswee.generic.zoxide role

- name: Setup
  tags:
    - zoxide.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - zoxide.install

    - name: Environment
      ansible.builtin.import_tasks: environment.yaml
      tags:
        - zoxide.environment


- name: Teardown
  ansible.builtin.import_tasks: teardown.yaml
  tags:
    - never
    - zoxide.teardown
...
