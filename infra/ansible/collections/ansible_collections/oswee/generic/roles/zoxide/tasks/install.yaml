---
# tasks/install.yaml file for oswee.generic.zoxide role

- name: install | Install system packages
  become: true
  ansible.builtin.dnf:
    name: '{{ item }}'
    state: present
  loop: '{{ packages.redhat }}'
...
