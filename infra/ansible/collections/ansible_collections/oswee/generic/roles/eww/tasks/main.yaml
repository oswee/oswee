---
# tasks/main.yaml file for oswee.generic.eww role

- name: Setup
  tags:
    - eww.setup
  block:
    - name: Build
      ansible.builtin.import_tasks: build.yaml
      tags:
        - eww.build

    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - eww.install

    # - name: Config
    #   ansible.builtin.import_tasks: config.yaml
    #   tags:
    #     - eww.config

- name: Teardown
  ansible.builtin.import_tasks: teardown.yaml
  tags:
    - never
    - eww.teardown
...
