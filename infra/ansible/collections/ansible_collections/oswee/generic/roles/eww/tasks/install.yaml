---
# tasks/install.yaml file for oswee.generic.eww role

- name: install | Copy Eww binary
  ansible.builtin.copy:
    src: '{{ eww_source_dir }}/target/release/eww'
    dest: '{{ ansible_env.HOME }}/.local/bin'
    mode: a+x
    remote_src: true
  when: eww_build.changed
...
