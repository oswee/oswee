---
# tasks/prepare.yaml file for oswee.generic.libdisplay_info role

- name: prepare | Install prerequisites
  become: true
  ansible.builtin.dnf:
    name: '{{ item }}'
    state: present
  loop: '{{ libdisplay_info_deps.redhat }}'

- name: prepare | Clone libdisplay-info repository
  ansible.builtin.git:
    repo: '{{ libdisplay_info_repo }}'
    dest: '{{ libdisplay_info_source_path }}'
    version: '{{ libdisplay_info_commit_hash | default("main") }}'
...
