---
# tasks/build.yaml file for oswee.generic.libliftoff role

- name: build | Meson build libliftoff
  ansible.builtin.command: meson setup build/
  args:
    chdir: '{{ libliftoff_source_path }}'

- name: build | Ninja build libliftoff
  ansible.builtin.command: ninja -C build/
  args:
    chdir: '{{ libliftoff_source_path }}'

# - name: build | Copy libliftoff library (version 0.1.0) to ldpath
#   become: true
#   ansible.builtin.copy:
#     src: '{{ libliftoff_source_path }}/build/libdisplay-info.so.1'
#     dest: /usr/local/lib
#     owner: root
#     group: root
#     mode: 0755
#     remote_src: true
...
