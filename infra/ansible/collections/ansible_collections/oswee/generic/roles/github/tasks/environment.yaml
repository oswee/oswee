---
# tasks file for github

- name: environment | Add environment
  become: true
  ansible.builtin.copy:
    dest: /etc/profile.d/github.sh
    mode: '0644'
    content: |
      export GITHUB_NPM_PAT=''  # To publish Node packages to GitHub registry
...
