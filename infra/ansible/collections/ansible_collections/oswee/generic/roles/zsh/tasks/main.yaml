---
# tasks/main.yaml file for oswee.generic.zsh role

- name: Setup
  tags:
    - zsh.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - zsh.install

    - name: Config
      ansible.builtin.import_tasks: config.yaml
      tags:
        - zsh.config

- name: Teardown
  ansible.builtin.import_tasks: teardown.yaml
  tags:
    - never
    - zsh.teardown
...
