---
# tasks file for Zk

- name: environment | Create environment
  become: true
  ansible.builtin.copy:
    dest: '{{ zk_environment_path }}'
    mode: '0644'
    content: |
      export ZK_NOTEBOOK_DIR="~/zet"
...
