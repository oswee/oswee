---
# tasks file for Zk

- name: Install
  import_tasks: install.yaml
  tags:
    - zk.setup

- name: Build
  import_tasks: build.yaml
  tags:
    - zk.install
    - zk.build

- name: Environment
  import_tasks: environment.yaml
  tags:
    - zk.setup
    - zk.setup.environment
