---
# tasks/main.yaml file for oswee.generic.zk role

- name: Setup
  tags:
    - zk.setup
  block:
    - name: Build
      ansible.builtin.import_tasks: build.yaml
      tags:
        - zk.build

    # - name: Install
    #   ansible.builtin.import_tasks: install.yaml
    #   tags:
    #     - zk.install

    - name: Environment
      ansible.builtin.import_tasks: environment.yaml
      tags:
        - zk.environment
...
