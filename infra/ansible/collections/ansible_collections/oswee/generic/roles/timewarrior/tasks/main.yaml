---
# tasks file for Timewarrior

- name: Setup
  tags:
    - timewarrior
    - timewarrior.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - install
        - timewarrior.install

    # - name: Config
    #   ansible.builtin.import_tasks: config.yaml
    #   tags:
    #     - config
    #     - timewarrior.config

# - name: Teardown
#   ansible.builtin.import_tasks: teardown.yaml
#   tags:
#     - never
#     - teardown
#     - timewarrior.teardown
...
