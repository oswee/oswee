---
# tasks file for Video

- name: Setup
  tags:
    - video
    - video.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - install
        - video.install

    # - name: Config
    #   ansible.builtin.import_tasks: config.yaml
    #   tags:
    #     - config
    #     - video.config

# - name: Teardown
#   ansible.builtin.import_tasks: teardown.yaml
#   tags:
#     - never
#     - teardown
#     - video.teardown
...
