---
# tasks file for Sway

- name: Setup
  tags:
    - sway.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - sway.install

# - name: Teardown
#   ansible.builtin.import_tasks: teardown.yaml
#   tags:
#     - never
#     - teardown
#     - sway.teardown
...
