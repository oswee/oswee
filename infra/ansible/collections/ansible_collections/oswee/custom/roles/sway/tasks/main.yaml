---
# tasks file for Sway

- name: Setup
  tags:
    - sway.setup
  block:
    - name: Config
      ansible.builtin.import_tasks: config.yaml
      tags:
        - sway.config

# - name: Teardown
#   ansible.builtin.import_tasks: teardown.yaml
#   tags:
#     - never
#     - sway.teardown
...
