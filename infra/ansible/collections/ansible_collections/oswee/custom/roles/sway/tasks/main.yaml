---
# tasks/main.yaml file for oswee.custom.sway role

- name: Setup
  tags:
    - sway.custom
  block:
    - name: Config
      ansible.builtin.import_tasks: config.yaml
      tags:
        - sway.config

    - name: Environment
      ansible.builtin.import_tasks: environment.yaml
      tags:
        - sway.environment

- name: Teardown
  ansible.builtin.import_tasks: teardown.yaml
  tags:
    - never
    - sway.teardown
...
