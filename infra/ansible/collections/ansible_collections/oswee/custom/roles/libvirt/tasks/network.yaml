---
# tasks file for Libvirt

- name: network | Undefine default network
  community.libvirt.virt_net:
    command: undefine
    name: default
    uri: '{{ connection_uri }}'

# - name: network | Redefine default network
#   community.libvirt.virt_net:
#     command: define
#     name: default
#     xml: '{{ lookup("file", "net-default.xml") }}'
#     uri: 'xen:///'

# - name: network | Start default network
#   community.libvirt.virt_net:
#     name: default
#     state: active
#     autostart: yes
#     uri: 'xen:///'
...
