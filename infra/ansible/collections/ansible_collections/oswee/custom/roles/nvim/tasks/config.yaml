---
# tasks/config.yaml file for oswee.custom.nvim role

- name: config | Copy all configuration files
  ansible.posix.synchronize:
    src: nvim/
    dest: '{{ nvim_config_path }}'
    archive: true
...
