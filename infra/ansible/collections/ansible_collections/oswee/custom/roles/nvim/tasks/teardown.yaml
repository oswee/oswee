---
# tasks/teardown.yaml file for oswee.custom.nvim role

- name: teardown | Remove Neovim directories
  ansible.builtin.file:
    path: '{{ item }}'
    state: absent
  loop:
    - '{{ nvim_config_path }}'
    - /home/{{ nvim_user }}/.local/share/nvim
...
