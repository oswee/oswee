---
# tasks/main.yaml file for oswee.custom.nvim role

- name: Setup
  tags:
    - nvim.setup
  block:
    - name: Config
      ansible.builtin.import_tasks: config.yaml
      tags:
        - nvim.config

    - name: Lsp
      ansible.builtin.import_tasks: lsp.yaml
      tags:
        - nvim.lsp

# - name: Teardown
#   ansible.builtin.import_tasks: teardown.yaml
#   tags:
#     - never
#     - nvim.teardown
...
