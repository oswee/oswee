---
# tasks/main.yaml file for oswee.custom.systemd role

- name: Setup
  tags:
    - systemd.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - systemd.install

    - name: Config
      ansible.builtin.import_tasks: config.yaml
      tags:
        - systemd.config

- name: Teardown
  ansible.builtin.import_tasks: teardown.yaml
  tags:
    - never
    - systemd.teardown
...
