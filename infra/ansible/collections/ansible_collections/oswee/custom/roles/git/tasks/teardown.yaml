---
# tasks/teardown.yaml file for oswee.custom.git role

- name: teardown | Remove files and directories
  ansible.builtin.file:
    path: '{{ item }}'
    state: absent
  loop:
    - $HOME/.gitconfig
...
