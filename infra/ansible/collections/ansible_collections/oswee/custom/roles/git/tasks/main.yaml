---
# tasks/main.yaml file for oswee.custom.git role

- name: Setup
  tags:
    - git.setup
  block:
    # - name: Install
    #   ansible.builtin.import_tasks: install.yaml
    #   tags:
    #     - git.install

    - name: Config
      ansible.builtin.import_tasks: config.yaml
      tags:
        - git.config

# - name: Teardown
#   ansible.builtin.import_tasks: teardown.yaml
#   tags:
#     - never
#     - git.teardown
...
