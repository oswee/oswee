---
# tasks/main.yaml file for oswee.custom.eww role

- name: Setup
  tags:
    - eww.setup
  block:
    - name: Config
      ansible.builtin.import_tasks: config.yaml
      tags:
        - eww.config

- name: Teardown
  ansible.builtin.import_tasks: teardown.yaml
  tags:
    - never
    - eww.teardown
...
