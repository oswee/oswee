---
# tasks/main.yaml file for oswee.custom.lvm role

- name: Setup
  tags:
    - lvm.setup
  block:
    - name: Install
      ansible.builtin.import_tasks: install.yaml
      tags:
        - lvm.install

    - name: Config
      ansible.builtin.import_tasks: config.yaml
      tags:
        - lvm.config

- name: Teardown
  ansible.builtin.import_tasks: teardown.yaml
  tags:
    - never
    - lvm.teardown
...
