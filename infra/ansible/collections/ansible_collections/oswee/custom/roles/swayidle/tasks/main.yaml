---
# tasks/main.yaml file for oswee.custom.swayidle role

- name: Setup
  tags:
    - swayidle.setup
  block:
    - name: Config
      ansible.builtin.import_tasks: config.yaml
      tags:
        - swayidle.config

- name: Teardown
  ansible.builtin.import_tasks: teardown.yaml
  tags:
    - never
    - swayidle.teardown
...
