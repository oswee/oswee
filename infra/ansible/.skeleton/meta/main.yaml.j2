---
# roles/{{ role_name | regex_replace('.*roles\/') }}/meta/main.yaml

galaxy_info:
  namespace: oswee
  role_name: {{ role_name | regex_replace('.*roles\/') }}
  version: 0.0.1
  author: Dzintars Klavins
  description: Ansible role to maintain {{ role_name | regex_replace('.*roles\/') | capitalize }}
  company: Oswee

  license: Apache-2.0

  min_ansible_version: 2.14

  platforms:
    - name: Fedora
      versions:
        - 37

  galaxy_tags:
    - {{ role_name | regex_replace('.*roles\/') }}
    - oswee
    - dzintars
    - fedora

dependencies: []
...
