---
environment: dev
tls_certs: '{{ vault_tls_certs }}'
tls_certs_dev: '{{ vault_tls_certs_dev }}'
dns_prod_domain_name: '{{ vault_dns_prod_domain_name }}'
dns_dev_domain_name: '{{ vault_dns_dev_domain_name }}'
dns_domain_search: '{{ vault_dns_prod_domain_name }}'
cluster_name: dev
cluster_network: 192.168.100
gateway_host_octet: '254'

# Reminders
# primary_dev_apex: '{{ vault_primary_dev_apex }}'
# primary_prod_apex: '{{ vault_primary_prod_apex }}'
# personal_apex: '{{ vault_personal_apex }}'
# community_apex: '{{ vault_community_apex }}'

# subdomains:
#   keycloak:
#   gerrit:
#   git:
#   heimdal:
#   api:
#   cockpit:
#   ci:
#   vault:
#   jenkins:
...
